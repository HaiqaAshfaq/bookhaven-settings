# Project Rules

1. Use separate validation functions for all forms so validation logic remains reusable and testable.

2. Every form input must have proper labels, clear error messages, and accessibility support.

3. Every new feature must include automated tests before considering the implementation complete.

4. Handle edge cases such as empty inputs, invalid formats, and unexpected user behavior.

5. Review responsive design and usability before submitting UI changes.